services:
  frontend:
    build:
      context: ./frontend
      args:
        VITE_APP_TITLE: ${VITE_APP_TITLE:-JagdSchießen}
    restart: unless-stopped
    networks:
      - web
      - internal        # braucht internal um /api/* ans Backend zu proxyen
    depends_on:
      - backend

  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}
      JWT_SECRET: ${JWT_SECRET}
      CORS_ORIGIN: ${CORS_ORIGIN}
      COOKIE_SECURE: ${COOKIE_SECURE:-false}
      NODE_ENV: production
    networks:
      - internal        # kein web-Netz — nur über Nginx erreichbar
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

networks:
  web:
    external: true
  internal:

volumes:
  db_data:
